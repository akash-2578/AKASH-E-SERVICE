PORT=3000

# Recipient mailbox for notifications
ALERT_EMAIL_TO=you@example.com

# SMTP credentials
# For Gmail, use your full Gmail address and a 16-char App Password.
# Spaces inside app passwords are allowed; the server strips them automatically.
SMTP_USER=your-email@gmail.com
SMTP_PASS=xxxx xxxx xxxx xxxx

# Optional sender display/address. Defaults to SMTP_USER.
SMTP_FROM="AKASH E SERVICE <your-email@gmail.com>"

# Optional custom SMTP server (leave blank for Gmail service mode)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=465
# SMTP_SECURE=true
